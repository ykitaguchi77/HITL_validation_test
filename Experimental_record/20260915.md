# 2026-09-15 実験記録

## 実験再開に向けた論文戦略の相談(3名アノテータ・筋書き・論文化リスク)(相談のみ・コード変更なし)

### 概要
- 約2か月ぶりに実験を再開するにあたり、ユーザーから
  「被検者3名でタスクを実施する予定。論文の筋書きはどうなるか、3名で十分か、論文化で問題になる点は何か」
  の3点を相談された。
- `Research_overview.md`・`scripts/prepare_experiment.py`・`outputs/models/heldout_dice.csv`・
  `Knowledge/hitl-annotation-efficiency-metrics-from-literature.md` を読み直して現状(設計・モデル・計測項目)を
  再把握した上で回答。ファイル変更・データ収集は行っていない。

### 1. 論文の筋書き(提示内容)
- **問い**: セグメンテーションモデルは何枚の学習データから アノテータの労力を実質的に減らし始め、
  どこで頭打ちになるか(=手描き→モデル支援へ切り替えるべき時点)。
- **タイトル案**: *How much training data does a segmentation model need to help its annotators?
  A blinded within-subject study of human-in-the-loop annotation efficiency versus model maturity in periocular images*
- **Methods 要約**: 3クラス(eyelid/iris/pupil)・患者リーク無し nested サブセット 100〜2500 枚で学習した
  SegFormer-B1 ×8(固定 held-out 463枚の Dice 0.919→0.958(ピーク seg2000)、500〜1000枚で飽和傾向)、
  独自 webapp、9条件(scratch + HITL×8)×10枚、3×9 Latin 方格、HITL 8条件は盲検混合提示、練習2種、
  末尾に同一条件再掲(Session 9, test-retest)。指標は主要=作業時間、副次=クリック/頂点編集/マウス移動、
  Dice/IoU、HD95/ASSD/Boundary-F1@2px、Paas 主観負荷、初動/アイドル時間、correction Dice。
- **統計**: 画像・アノテータを含む混合効果モデル。**主解析は log(学習枚数) に対する用量反応(トレンド)**とし、
  8条件の総当たり比較にはしない。scratch との対比は別建て。品質は非劣性マージン(例 Dice 差 −0.01 以内)を事前設定。
- **Results 図表案**: ①デザイン図 ②モデル Dice vs 学習枚数 ③作業時間/クリック/主観負荷 vs 学習枚数
  (scratch を水平基準線で併記、**飽和点 knee が主結果**) ④最終品質 vs 学習枚数 ⑤オートメーションバイアス
  (correction Dice と最終 Dice の相関、scratch 比較) ⑥補助(ICC・アノテータ別・修正労力 vs 品質曲線)。
- **Discussion の核**: 効率の飽和点がモデル Dice の飽和点と一致するか遅れるか。横軸を枚数だけでなく
  **held-out Dice** でも報告すると他タスクへ転用可能な指針になる。

### 2. 被検者3名で十分か(結論: 最低限成立、主張の書き方に制約)
- 被験者内・画像反復型で解析単位は画像アノテーション(本解析 270 + 再掲 30)。scratch vs HITL のような
  大きな差は3名で検出可。**隣接条件(例 seg1500 vs seg2000、各30観測)の対比は検出力不足**なので
  トレンド解析を主にする方針が3名設計の生命線。
- 先行研究も2〜5名が多く(文献ノートでは Vision6D の11名が例外)、3名は査読で即却下される数ではない。
- **統計上の真の問題**: 水準3のランダム効果は分散推定が不安定 → アノテータは**固定効果**として扱い
  「結論はこの3名に対するもの」と限定。3名の経歴(専門医/研修医/技師等)と異質性を明記。
- **拡張余地**: `scripts/prepare_experiment.py:195` 付近の Latin 方格は cyclic で `ANNOTATOR_NAMES` を
  増やすだけで最大9行まで拡張可。査読で人数を求められた場合、画像セットを変えずに4〜6人目を追加できる
  (追加後に既存3名の割当が不変であることを再生成後に検証する必要あり)。可能なら最初から5〜6名が望ましい。

### 3. 論文化で問題になりうる点(優先度順)
1. **scratch が常にセッション0(非盲検・最初)** → 習熟が浅い時期に scratch を行うため scratch が遅く見え
   HITL 効果を過大評価する方向のバイアス。対策案 (a) **最終セッションとして scratch 10枚を追加**(前後差から
   学習効果を推定、1人 10〜20分の追加負担・推奨) (b) 少なくとも scratch 内スロット順の時間推移を報告。
2. **GT スタイル継承バイアス** → GT は単一作成者、モデルはその GT で学習しているため HITL 成果物が GT の癖に
   寄り GT-Dice で有利。境界指標併記・Discussion で明示。scratch 画像の一部を3名共通にすると人間間ばらつきの
   上限が得られる。
3. **主要評価項目と多重性** → 指標が約10あるので主要(作業時間)/副次を**データ収集前に文書化**(軽量事前登録)。
4. **倫理審査** → 患者画像二次利用に加え、アノテータが研究参加者になるため同意手続きが必要。
5. **学習曲線が単一 run** → 100〜500 だけでも seed を変えて2〜3回学習し誤差棒を付けると説得力向上。
6. **U-Net ablation** → 人間実験を伴わない Dice 曲線のみなら価値限定的。スコープを絞るなら落としてよい。
7. **疲労・分割実施** → 1人約106枚で2〜3.5時間。実施日・休憩をログに残し複数日運用を決めておく。
8. 軽微: タイミング・サイドチャネル対策(予測事前計算)未実装だがウォームアップで実害小、
   Boundary-F1@2px は瞳孔で甘めに出る点は既知。
- 投稿先の目安: TVST / Ophthalmology Science / Scientific Reports / IJCARS / CMPB。

### 次の一手(提示・ユーザー判断待ち)
- (1) 事前解析計画書(主要/副次指標・モデル式・非劣性マージン)を `outputs/` に作成
- (2) 末尾 scratch セッション追加の設計変更(`scripts/prepare_experiment.py`)
- のどちらかから着手可能と提示してセッション終了。

## 追記: 虹彩・瞳孔アノテーションが課題に含まれているかの確認(Q&A・コード変更なし)

### 概要
- ユーザーから「虹彩や瞳孔のアノテーションも含まれていたか」の確認質問。
  `scripts/webapp/backend/config.py:46` を参照して **Eyelid / Iris / Pupil の3クラス**が現行課題であることを確認
  (Caruncle は 6/18 に Eyelid へ統合して廃止済み)。

### 確認した内容
| クラス | 図形 | 操作 | GT/モデル |
|---|---|---|---|
| Eyelid | ポリゴン | 頂点ドラッグ・点追加・複数選択移動 | CVAT の Eyelid∪Caruncle 統合外形 / SegFormer eyelid チャネル |
| Iris | 回転楕円(OBB) | 移動・リサイズ・回転 | CVAT 楕円 / iris チャネルから楕円フィット |
| Pupil | 回転楕円(OBB) | 同上 | 同上 / pupil チャネル |

- HITL 条件では3クラスすべてがモデル予測でプレフィルされ、scratch では3つとも手描き。
- 3クラス揃わないと提出できないガードあり(実験96画像・候補プール463枚で瞳孔欠損ゼロを検証済み)。
- 計測・スコアは per-class(Dice/IoU、HD95/ASSD/Boundary-F1@2px は楕円も `cv2.ellipse` 塗りつぶしで境界算出、
  クラス別クリック/頂点編集/時間内訳も記録)。周辺視3枚/セッションは楕円の傾き操作を確実に発生させる目的。
- モデル held-out Dice も3クラス別(例 seg2500: eyelid 0.975 / iris 0.961 / pupil 0.927 — 瞳孔が最難)。

### 論文への含意
- 「ポリゴン修正(眼瞼)」と「楕円パラメータ修正(虹彩・瞳孔)」は労力の質が異なるため、
  **クラス別に効率曲線を出す**のが自然。瞳孔は小さく Boundary-F1@2px が甘めに出るので HD95/ASSD の絶対 px 値を併記する方針。

### 関連リンク(追記分)
- `scripts/webapp/backend/config.py`(クラス定義・図形種別)
- `scripts/webapp/backend/scoring.py`(per-class 指標・楕円マスク化)

## 追記2: クラス別の手間(労力)をログから計算できるかの確認(Q&A・コード変更なし)

### 概要
- ユーザーから「クラス別にどれぐらいの手間がかかったかはログから計算可能か」の確認質問。
  `scripts/webapp/static/js/metrics.js`・`scripts/webapp/backend/session.py`・`scripts/webapp/static/js/app.js`
  を読み直し、**既にクラス別の労力指標が `summary.csv` の列としてそのまま保存されている**ことを確認して回答。

### 確認した内容(eyelid / iris / pupil それぞれ、`session.py` の `_per_class_cols()`)
| 列 | 意味 |
|---|---|
| `{k}_time_sec` | そのクラスを選択していた作業時間(一時停止分は除外) |
| `{k}_clicks` | そのクラス選択中のクリック数 |
| `{k}_vertices_edited` | 頂点の追加・移動・削除数 |
| `{k}_correction_dice` / `{k}_area_change` | HITL でプレフィルをどれだけ変えたか(修正量) |
| `{k}_dice` / `_iou` / `_hd95` / `_assd` / `_boundary_f1` | 品質 |
| `{k}_gt_present` / `{k}_submitted_present` | GT/提出物の有無 |

- JSONL の `events[]` は全操作が `{t, type, cls}` 形式でタイムスタンプ付きなので、クラス別の
  「初動時間」「アイドル時間」「編集数の時系列(修正労力 vs 品質曲線)」もオフラインで再構成可能。

### 仕組みと解釈上の注意点
- 時間の帰属は「右サイドバーで選択中のクラス」で決まる(`metrics.js` の `setActiveClass`/`_flushClassTime`)。
  クラス選択はサイドバーのみ(canvas クリックでは切り替わらない)ので帰属は明確。
- **画像表示直後は eyelid が既定で選択される**(`app.js` の `this.setActiveClass("eyelid")`)ため、
  全体を眺める・ズームする等の「クラス非依存の時間」は eyelid に乗る。3クラス合計は `duration_sec` と一致するはずだが
  **eyelid はやや過大評価**される(events から「最初の操作までの時間」を差し引く補正は可能)。
- 楕円(iris/pupil)の移動・リサイズ・回転は「頂点編集」ではないので `{k}_vertices_edited` には乗らず、
  時間・クリック・correction_dice で見る。
- `mouse_distance_px` は全体のみでクラス別はない。必要なら events の `cls` で mouseMove を帰属させる小改修が要る。

### 結論
- 「眼瞼(ポリゴン修正)と虹彩・瞳孔(楕円修正)で、モデル成熟に伴う手間の減り方が違うか」を示す材料は
  **現行ログで揃っている**。追加実装は不要(クラス別マウス距離が欲しい場合のみ小改修)。

### 関連リンク(追記2分)
- `scripts/webapp/static/js/metrics.js`(クラス別 時間/クリック/頂点編集の帰属ロジック)
- `scripts/webapp/backend/session.py`(`_per_class_cols()`=summary.csv のクラス別列)
- `scripts/webapp/static/js/app.js`(画像表示直後の既定クラス=eyelid)
- `Knowledge/per-class-effort-attribution-by-active-class.md`(本追記の知見を新規作成)

## 追記3: 被験者の立場からタスク実施の実際を追体験し、実施前に潰すべき手順書/運用の穴を抽出(ウォークスルー・コード変更なし)

### 概要
- ユーザー依頼「被験者のタスクの実際について、被験者のつもりになって話してください」に対し、
  `scripts/webapp/annotator_guide.md` を読み直し、練習1(Scratch 3枚)→練習2(修正 3枚)→本番 Scratch(10枚)→
  Session 1〜9(各10枚・盲検 HITL、S9 は再掲)の全12セッション・106枚を被験者(例: maeda)視点で追体験して報告。
  ファイル変更は行っていない。

### 追体験で描いた流れ(要点)
- ログイン: 配布 URL → Basic 認証(`annotator`/共有パスワード) → アノテータ選択(先頭の「動作テスト用」を選ばないよう注意)。
- 練習1 Scratch: Eyelid が既定選択。ポリゴン(P)で瞼縁を1周(涙丘も含めて内眼角側を大きめに)、Shift+クリックで点追加・ドラッグ微調整、
  Iris/Pupil は楕円(E)。周辺視画像では Transformer で楕円を回転させる必要があり**ここが最も戸惑う操作**。
  提出後の確認画面で GT 点線と Dice が出て自分の癖(外側を広く取りすぎ等)が分かる → 負担度(Paas 1〜9)。
- 練習2 修正: 「推論中…」後に3クラスがプレフィルされ、数点ドラッグ+楕円微調整で1枚1分未満。手描きより明らかに楽。
- 本番 Scratch 10枚: 20〜30分、7枚目あたりで疲労。GT 表示なし。負担度は 5〜7、周辺視は 7〜8 になりがち。
- Session 1〜9: 画像ごとに予測の出来が大きく違う(数点直すだけで 20〜30秒 / 瞼縁がガタガタ・瞳孔楕円が反射光に引かれてずれる)。
  モデル名が出ないので「今日は弱いモデル」とは思えず1枚ごとに判断。「直すより消して描き直す方が早いのでは」と迷う場面がある。
  10枚ごとに一覧へ戻り「✓ Session N (10枚)」、休憩・別日継続が可能。フォーカス喪失で一時停止オーバーレイ。S9 は他と区別がつかない。
- 総負担: 練習6 + 本番100 = 106枚、1枚 1〜1.5分で**合計 2〜3時間**。Scratch+2セッションを1日目、残りを2〜3日に分ける運用が現実的。

### 被験者として引っかかった点(実施前に潰せるもの)
1. **ガイドの記載が古い**: `annotator_guide.md` L14 は「アノテータ番号 `1`/`2`/`3` を選択」だが、実画面は実名
   (`prepare_experiment.py` の `ANNOTATOR_NAMES = ["kubota","maeda","kaisho"]`、2026-07-13 に変更済み)。混乱の元。
2. **「直すか描き直すか」の方針が未規定**: 予測が悪いとき 削除→描き直し を許すか必ず修正するかで労力の計測(頂点編集数・
   correction dice の意味)が変わる。「どちらでも良い、早いと思う方法で」等を**明示し、解析方針も事前に決める**べき。
3. **周辺視の楕円回転**は練習3枚中1枚しか経験できない → 練習の「もう一度やる」で慣れるよう案内があると安心。
4. **精度の基準**: 「ふだん通りの精度で」とあるが目標精度の感覚は練習の GT 表示でしか掴めない。
   「GT との差 2px 以内なら一致扱い」と伝えるかどうかは検討事項。
5. **休憩・分割実施の目安**(1日に何セッション)がガイドに無い。

### 次の一手(提示・ユーザー判断待ち)
- 上記 1(アノテータ選択の記載を実名へ更新)と 2(修正/描き直し方針の明記)を `annotator_guide.md` で実施前に直すことを提案し、
  「修正しましょうか?」と確認してセッション終了(未着手)。

### 関連リンク(追記3分)
- `scripts/webapp/annotator_guide.md`(被験者向け手順書。L14 のアノテータ番号記載が古い)
- `scripts/prepare_experiment.py`(`ANNOTATOR_NAMES` 実名3名・12セッション構成)
- `Knowledge/participant-walkthrough-before-study-launch.md`(本追記の知見を新規作成)

## 追記4: `annotator_guide.md` の実施前修正 ①アノテータ選択の実名化 ②修正/描き直し方針の明記(手順書のみ変更・コード変更なし)

### 概要
- 追記3 で提案した5点のうち、ユーザー承認「はい、1と2を修正してください」を受けて
  `scripts/webapp/annotator_guide.md` を2箇所修正した。コード・割付・experiment.json は不変。
- 事前に `main.py` の `UI_ANNOTATORS = ["test"] + list(ANNOTATORS)` を確認し、UI 上は先頭が「動作テスト用(本番に含めない)」、
  続いて実名3名(kubota/maeda/kaisho)が並ぶことを裏取りしてから記載を合わせた。

### 変更内容(`scripts/webapp/annotator_guide.md`)
1. **ログイン手順(§1 手順3)**: 「アノテータ番号 `1`/`2`/`3` を選択」→「**自分の名前**(`kubota` / `maeda` / `kaisho`)を選択」。
   一番上の「動作テスト用(本番に含めない)」を選ばないこと、**名前を選び間違えると別人の割当で作業してしまう**ため必ず確認、を追記。
2. **作業の流れ(§2)**: 「**予測の直し方は自由**。頂点を動かして直しても、図形を削除(🗑 / Del)してゼロから描き直しても構わない。
   画像ごとに自分が一番早く正確に仕上げられる方法を選ぶ(どちらが正しいということはない)」を追記。
- 編集は python の `str.replace` + `assert old in s` で対象文を厳密一致させて適用し、`git diff --stat`(1 file, +16/−6)で確認。

### 解析への含意(記録しておく判断)
- 方針2を「自由」にしたことで、HITL 条件の `shapes_deleted > 0` は「予測を捨てて描き直した」ケースとして識別できる。
  **「描き直し率 vs 学習枚数」を副次指標として報告**できる(低データモデルほど描き直しが増える想定)。
  描き直し画像では `{k}_vertices_edited` / `{k}_correction_dice` の意味が変わるため、事前解析計画に
  「描き直し画像の扱い(全体解析に含める+描き直し率を別報告)」を明記する。

### 注意(git)
- 今回の diff には 7/12 セッションで更新済みだった未コミット分(Session 1〜9 化・確認画面/負担度フロー・
  Ctrl+ドラッグ複数頂点選択・フォーカス喪失時の一時停止の記述)も含まれる。`annotator_guide.md` は
  丸ごと未コミットのため、実施前に webapp 一式とまとめてコミットしておくことを推奨(ユーザーへ伝達済み)。

### 未対応(追記3 の残り3点・ユーザー判断待ち)
- ③周辺視の楕円回転を練習「もう一度やる」で慣れる案内 ④目標精度(px)の伝え方 ⑤休憩・分割実施の目安。

### 関連リンク(追記4分)
- `scripts/webapp/annotator_guide.md`(修正した手順書)
- `scripts/webapp/backend/main.py`(`UI_ANNOTATORS`=UI の選択肢順を確認)
- `Knowledge/participant-walkthrough-before-study-launch.md`(追記: 「自由」方針→描き直し率を副次指標化)

## 追記5: 最終セッションに scratch を再掲する案の検討(設計相談・コード変更なし・ユーザー判断待ち)

### 概要
- ユーザーから「最終セッションでスクラッチをもう一度やる案についてはどうでしょうか?」の相談。
  追記1 の論文化リスク①(scratch がセッション0固定=習熟が浅い時期に測られ HITL 効果を過大評価)への対策として
  **推奨する**と回答し、画像の選び方3案を比較・解析での使い方・実装見通しを提示した。ファイル変更なし。

### なぜ必要か
- 現設計では scratch の時間が「ツールに慣れる前」の値になり、査読者は必ず「scratch が遅いのは順序効果では?」と問う。
- 末尾に scratch を再度置くと**同一人物の「習熟前 vs 習熟後の scratch」**が得られ、順序効果の大きさそのものを報告できる。
  1人あたり 20〜30 分の追加負担で設計最大の弱点を潰せる。

### 画像の選び方(3案比較)
| 案 | 画像 | 得られるもの | 弱点 |
|---|---|---|---|
| **A: セッション0と同じ10枚を再描画(推奨)** | 既出 scratch 画像 | 順序効果を**画像で対応づけた差**として最も高感度に推定(30ペア)。scratch の個人内再現性(Session 9 の HITL 版と対になる) | 画像の記憶。ただし100枚・数時間〜数日を挟んだ手描きへの影響は小さい |
| B: 新規10枚を3人共通ブロックに | 未使用画像(プール余裕あり) | **3人の scratch 間一致度**=人間間ばらつきの上限(GT スタイル継承バイアスの議論材料) | 順序効果と画像難易度が交絡(その10枚は「末尾 scratch」でしか観測されないため分離不能) |
| C: HITL で見た画像10枚を scratch で | 既出 HITL 画像 | 同一画像での scratch vs HITL 対比較 | 直前に直した予測の記憶が強く残り scratch として汚れる |

- **A を推奨**。本論文の主張(HITL の効率利得)に直接効くのは順序効果の補正で、A がそれを最もきれいに推定できる。
  B の人間間一致度は魅力的だが順序効果と分離できない設計は本末転倒。A+B 併用は1人 120枚超で負担過大。

### 解析での使い方
- 主解析 `duration ~ condition + ...` の scratch 基準値は**末尾 scratch(習熟後)**を使うのが保守的(HITL 効果を控えめに見積もる)。
  最初の scratch との差は「順序効果」として別に報告。
- 末尾は疲労が乗るため順序効果は過小評価側(=さらに保守的)。この方向性は Discussion で書ける。
- scratch の ICC を Session 9 の HITL ICC と並べて報告可能。

### 実装の見通し(未着手)
- `scripts/prepare_experiment.py` に最終セッション「Scratch(2回目)」を追加(セッション0と同じ10枚を**専用 RNG**で再シャッフル、
  既存90枚・練習・Session 9 の割当は不変のまま再生成し audit で確認 → `Knowledge/seeded-design-additive-change-dedicated-rng.md` の方式)。
- scratch は元々非盲検なので UI 上「Scratch(2回目)」と表示して問題なし。Session 9 の後(全 HITL 終了後)に固定配置。
- 記録は `is_repeat=true` + `condition=scratch` で区別。backend/front は既存の再掲機構(Session 9)をほぼ流用、1時間程度で実装・検証できる見込み。
- `scripts/webapp/annotator_guide.md` は「全12セッション」→「全13セッション」へ更新が必要。

### 次の一手(ユーザー判断待ち)
- 案 A で進めてよければ実装(3ステップ超の設計変更のため着手時にプランを提示してから実装)。

### 関連リンク(追記5分)
- `scripts/prepare_experiment.py`(セッション構成・Session 9 再掲機構の流用元)
- `scripts/webapp/annotator_guide.md`(セッション数の更新が必要)
- `Knowledge/hitl-blinding-and-confound-design.md`(追記: 再掲基準セッションの画像選択=対応づけ優先)
- `Knowledge/seeded-design-additive-change-dedicated-rng.md`(割付追加時の専用 RNG 方式)

## 追記6: 最終セッション「Scratch（2回目）」の実装・`experiment.json` 再生成・実 API 検証(案A・設計変更・完了)

### 概要
- 追記5 の3案に対しユーザーが「**Aで行きましょう**」と決定。プランモードで計画(`~/.claude/plans/declarative-swinging-rose.md`)を提示・承認後に実装。
  全 HITL セッション(Session 9=盲検再掲)の後に、**セッション0と同じ10枚を再度ゼロから描く「Scratch（2回目）」**を追加し、
  同一人物・同一画像の「習熟前 vs 習熟後 scratch」(30ペア)で順序効果を推定できる設計にした。
- 事前調査で backend(`main.py`)/frontend(`app.js`)が完全に `experiment.json` 駆動(セッション一覧・順次アンロック・`model=None`→空プレフィル・
  `is_repeat` の記録透過)であることを確認し、**コード変更は `prepare_experiment.py` と配布資料のみ**で済ませた。

### 変更内容
1. **`scripts/prepare_experiment.py`**
   - 定数 `SCRATCH_REPEAT_KEY = "scratch_repeat"` / `SCRATCH_REPEAT_LABEL = "Scratch（2回目）"` を追加(docstring も更新)。
   - `main()` の各アノテータループで、HITL 再掲 `hitl_repeat` の append 直後に**専用 RNG `random.Random(SEED + 2000 + ai)`**で
     `scratch_imgs` のコピーを再シャッフルし `is_repeat=True` を付与(`condition="scratch"`, `model=None`, `train_size=None` は不変)、
     `sess(..., N_BLOCKS + 1, SCRATCH_REPEAT_KEY, is_hitl=False, label)` を末尾に append(`session_index=10`)。
     メイン `rng` の消費順を変えないため既存90枚・練習・Session 9 の割当は不変。
   - `_audit_and_assert` を拡張: `repeat` を HITL 再掲(`is_hitl`)と scratch 再掲(`not is_hitl`)に分け各1件を assert、`real`=9 は維持。
     scratch 再掲は **最終セッション(`sessions[-1]`)・`is_hitl=False`・全 item `condition=="scratch"` かつ `is_repeat`・image_id 集合==セッション0・
     7F+3P・提示順がセッション0と異なる・`session_index` が 0..N_BLOCKS+1 の連番**を assertion。
2. **`scripts/webapp/config/experiment.json` 再生成**: `ALL ASSERTIONS PASSED`。再生成前に scratchpad へバックアップし新旧 JSON を比較 →
   `blocks`/`practice`/`latin_square`/`models` と各アノテータの `sessions[0..9]` が**完全一致**、末尾1セッションのみ追加(3アノテータとも)。
3. **`scripts/webapp/annotator_guide.md`**: 「全12セッション」→「**全13セッション**」、流れの表に `| 13 | Scratch（2回目） | 本番・最後にもう一度ゼロから作成（10枚） |` を追加、
   「最後の Scratch（2回目）はツールに慣れた後の基準を測るためのもので、最初の Scratch と同じように描けばよい」を追記。
4. **backend / frontend はコード変更なし**(データ駆動)。`tasks/todo.md` に項目・レビューを追記。

### 検証(ローカル・認証なしで uvicorn 起動 → 実 API)
- `GET /api/run/kubota` → **13セッション**、末尾が `scratch_repeat` / `Scratch（2回目）` / `is_hitl=false` / `n_images=10`、露出キーは
  `key,label,is_hitl,is_practice,n_images` のみ(condition/model/is_repeat 非露出=盲検維持)。
- `GET /api/session/kubota/scratch_repeat` → 10枚、image_id 集合がセッション0と一致(順序は異なる)。
- `POST /api/predict`(scratch_repeat) → `initial` 空(プレフィルなし)。
- `test` アノテータで `POST /api/submit` を1件 → `summary.csv` に `condition=scratch, is_repeat=True, session_index=10` を確認 → 検証記録は削除。
- 検証サーバは `netstat -ano` で PID 特定 → `taskkill` で停止しポート解放(`Knowledge/stop-background-verification-server-to-free-port.md` の運用)。
- 途中の小トラブル: `str.replace` + `assert old in s` 方式のパッチが audit ブロックの行継続 `\` 部分で不一致(AssertionError)→ `Read` + `Edit` ツールで
  該当ブロックを厳密一致させて適用し直した(ファイルは壊れていない)。

### 運用上の注意(ユーザーへ伝達済み)
- `experiment.json` は起動時ロードのため、**本番前に `run_server.bat` で再起動**が必要。
- 1人あたりの負担は 106 → **116枚**(約 +20〜30分)。
- 7/12〜今日分の未コミット変更が多数溜まっているため、実施前に一括コミットを推奨。

### 解析への含意
- 主解析 `duration ~ condition + ...` の scratch 基準値は**末尾 scratch(習熟後)**を使う(HITL 効果を保守的に見積もる)。
  初回 scratch との画像対応づけ差(30ペア)を「順序効果」として別報告。末尾は疲労が乗るため順序効果は過小評価側=さらに保守的。
- scratch の個人内再現性(ICC)を Session 9 の HITL 版 ICC と並べて報告可能。

### 関連リンク(追記6分)
- `scripts/prepare_experiment.py`(`SCRATCH_REPEAT_KEY/LABEL`・専用 RNG `SEED+2000+ai`・audit 拡張)
- `scripts/webapp/config/experiment.json`(再生成・13セッション/アノテータ、git 管理外)
- `scripts/webapp/annotator_guide.md`(全13セッション構成)
- `tasks/todo.md`(実施前の設計・資料調整 セクション)
- `Knowledge/seeded-design-additive-change-dedicated-rng.md`(追記: 2回目の適用・新旧 JSON diff による不変性確認)
- `Knowledge/hitl-blinding-and-confound-design.md`(追記: 末尾基準セッションの実装形)

### 関連リンク
- `Research_overview.md`(現状把握の起点)
- `scripts/prepare_experiment.py`(Latin 方格・`ANNOTATOR_NAMES`・セッション構成)
- `outputs/models/heldout_dice.csv`(8モデルの固定 held-out Dice)
- `Knowledge/hitl-annotation-efficiency-metrics-from-literature.md`(先行研究の被験者数・指標)
- `Knowledge/hitl-blinding-and-confound-design.md`(本セッションの知見を追記)
